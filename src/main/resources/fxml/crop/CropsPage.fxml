<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>

<VBox spacing="35"
      alignment="TOP_LEFT"
      xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="tn.esprit.crop.controller.CropsController"
      stylesheets="/styles/styles.css"
      prefHeight="600.0"
      prefWidth="900.0"
      style="-fx-background-color: linear-gradient(to bottom right, #f9fbfd, #e8f5e9);">

    <padding>
        <Insets top="40" right="45" bottom="40" left="45"/>
    </padding>

    <!-- Header -->
    <HBox alignment="CENTER_LEFT" spacing="25">

        <!-- Left Title Section -->
        <VBox spacing="8" HBox.hgrow="ALWAYS">

            <Label text="🌿 Crops Management"
                   style="-fx-font-size: 28px;
                          -fx-font-weight: bold;
                          -fx-text-fill: #1b5e20;
                          -fx-letter-spacing: 1px;"/>

            <Label text="Select a crop below"
                   style="-fx-text-fill: #78909c;
                          -fx-font-size: 14px;"/>
        </VBox>

        <!-- Right Buttons Section -->
        <VBox spacing="16" alignment="CENTER_RIGHT">

            <!-- Add Crop -->
            <Button onAction="#handleAddCrop"
                    style="-fx-background-color: linear-gradient(to right, #2e7d32, #66bb6a);
                           -fx-text-fill: white;
                           -fx-font-size: 14px;
                           -fx-font-weight: bold;
                           -fx-background-radius: 40;
                           -fx-padding: 12 28;
                           -fx-cursor: hand;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.18), 15, 0, 0, 6);">

                <graphic>
                    <HBox spacing="10" alignment="CENTER">
                        <Text text="+"
                              style="-fx-font-size: 18px; -fx-fill: white;"/>
                        <Label text="Add New Crop"
                               style="-fx-text-fill: white;"/>
                    </HBox>
                </graphic>
            </Button>

            <!-- Manage Tasks -->
            <Button onAction="#goToTaskPage"
                    style="-fx-background-color: white;
                           -fx-border-color: #2e7d32;
                           -fx-border-width: 2;
                           -fx-text-fill: #2e7d32;
                           -fx-font-weight: bold;
                           -fx-font-size: 13px;
                           -fx-background-radius: 40;
                           -fx-border-radius: 40;
                           -fx-padding: 11 26;
                           -fx-cursor: hand;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 10, 0, 0, 3);">

                <graphic>
                    <HBox spacing="10" alignment="CENTER">
                        <Text text="📋"/>
                        <Label text="Manage Tasks"/>
                    </HBox>
                </graphic>
            </Button>

            <!-- Admin Table -->
            <Button onAction="#handleManageCrops"
                    style="-fx-background-color: white;
                           -fx-border-color: #1976d2;
                           -fx-border-width: 2;
                           -fx-text-fill: #1976d2;
                           -fx-font-weight: bold;
                           -fx-font-size: 13px;
                           -fx-background-radius: 40;
                           -fx-border-radius: 40;
                           -fx-padding: 11 26;
                           -fx-cursor: hand;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 10, 0, 0, 3);">

                <graphic>
                    <HBox spacing="10" alignment="CENTER">
                        <Text text="🛠"/>
                        <Label text="Admin Table"/>
                    </HBox>
                </graphic>
            </Button>

        </VBox>

    </HBox>

    <!-- Crop Cards Section -->
    <ScrollPane fitToWidth="true"
                VBox.vgrow="ALWAYS"
                hbarPolicy="NEVER"
                style="-fx-background-color: transparent;
                       -fx-background: transparent;
                       -fx-padding: 0;
                       -fx-border-color: transparent;">

        <content>
            <FlowPane fx:id="cropContainer"
                      hgap="35"
                      vgap="35"
                      prefWrapLength="850"
                      style="-fx-padding: 15;
                             -fx-background-color: transparent;"/>
        </content>

    </ScrollPane>

</VBox>
